#pragma once

#define NOOF_THREADS_CLEAR_BUFFER (64)
#define NOOF_THREADS_KILL_INSTANCES (64)
#define NOOF_THREADS_SCAN_INSTANCES (64)
#define NOOF_THREADS_SCAN_GROUPS (1024)
#define NOOF_THREADS_COPY_INSTANCES (128)
#define NOOF_THREADS_SCAN_COMMANDS (1024)

#define INSTANCE_COMPACTION_SCAN_BLOCK (NOOF_THREADS_SCAN_INSTANCES << 1)
#define INSTANCE_COMPACTION_NOOF_BLOCK (NOOF_THREADS_SCAN_GROUPS << 1)

#define MAX_NOOF_INSTANCES (INSTANCE_COMPACTION_SCAN_BLOCK * INSTANCE_COMPACTION_NOOF_BLOCK)
#define MAX_NOOF_MESHES (1 << 11)

#define GR_WORKGRAPH

// ------------------ MyMesh ------------------
// #define DEBUG_CULL_DETERMINISTIC(mesh_id, idx) (mesh_id == 1 && (idx & 1))
#define DEV_LOAD_INSTANCE_BLOB
#define DEV_LOAD_INSTANCE_BLOB_RANDOM

